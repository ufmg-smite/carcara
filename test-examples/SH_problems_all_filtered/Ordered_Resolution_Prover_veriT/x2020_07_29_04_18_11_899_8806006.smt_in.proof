(assume a0 (not (= (subst_cls$ subst_atm$ (plus$ (sum_mset$ (mset$ cs0$)) d0$) (comp_subst$ tau$ phi$)) (subst_cls$ subst_atm$ (plus$ (sum_mset$ (mset$ cs0$)) d0$) (comp_subst$ eta$ sigma$)))))
(assume a1 (= (comp_subst$ tau$ phi$) (comp_subst$ eta$ sigma$)))
(step t3 (cl (not (= subst_atm$ subst_atm$)) (not (= (plus$ (sum_mset$ (mset$ cs0$)) d0$) (plus$ (sum_mset$ (mset$ cs0$)) d0$))) (not (= (comp_subst$ tau$ phi$) (comp_subst$ eta$ sigma$))) (= (subst_cls$ subst_atm$ (plus$ (sum_mset$ (mset$ cs0$)) d0$) (comp_subst$ tau$ phi$)) (subst_cls$ subst_atm$ (plus$ (sum_mset$ (mset$ cs0$)) d0$) (comp_subst$ eta$ sigma$)))) :rule eq_congruent)
(step t4 (cl (= subst_atm$ subst_atm$)) :rule eq_reflexive)
(step t5 (cl (not (= (plus$ (sum_mset$ (mset$ cs0$)) d0$) (plus$ (sum_mset$ (mset$ cs0$)) d0$))) (not (= (comp_subst$ tau$ phi$) (comp_subst$ eta$ sigma$))) (= (subst_cls$ subst_atm$ (plus$ (sum_mset$ (mset$ cs0$)) d0$) (comp_subst$ tau$ phi$)) (subst_cls$ subst_atm$ (plus$ (sum_mset$ (mset$ cs0$)) d0$) (comp_subst$ eta$ sigma$)))) :rule th_resolution :premises (t3 t4))
(step t6 (cl (= (plus$ (sum_mset$ (mset$ cs0$)) d0$) (plus$ (sum_mset$ (mset$ cs0$)) d0$))) :rule eq_reflexive)
(step t7 (cl (not (= (comp_subst$ tau$ phi$) (comp_subst$ eta$ sigma$))) (= (subst_cls$ subst_atm$ (plus$ (sum_mset$ (mset$ cs0$)) d0$) (comp_subst$ tau$ phi$)) (subst_cls$ subst_atm$ (plus$ (sum_mset$ (mset$ cs0$)) d0$) (comp_subst$ eta$ sigma$)))) :rule th_resolution :premises (t5 t6))
(step t8 (cl) :rule resolution :premises (t7 a0 a1))
