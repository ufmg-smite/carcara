(assume a0 (not (member$ c_mu$ (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))))))))
(assume a1 (and (= c_mu$ (subst_cls$ subst_atm$ c$ mu$)) (is_ground_subst$ subst_atm$ mu$)))
(assume a2 (member$ (subst_cls$ subst_atm$ c$ mu$) (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))))))
(step t4 (cl (= c_mu$ (subst_cls$ subst_atm$ c$ mu$))) :rule and :premises (a1))
(step t5 (cl (not (= c_mu$ (subst_cls$ subst_atm$ c$ mu$))) (not (= (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))))) (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))))))) (not (member$ (subst_cls$ subst_atm$ c$ mu$) (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))))))) (member$ c_mu$ (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))))))) :rule eq_congruent_pred)
(step t6 (cl (= (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))))) (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))))))) :rule eq_reflexive)
(step t7 (cl (not (= c_mu$ (subst_cls$ subst_atm$ c$ mu$))) (not (member$ (subst_cls$ subst_atm$ c$ mu$) (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))))))) (member$ c_mu$ (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))))))) :rule th_resolution :premises (t5 t6))
(step t8 (cl) :rule resolution :premises (t7 a0 t4 a2))
