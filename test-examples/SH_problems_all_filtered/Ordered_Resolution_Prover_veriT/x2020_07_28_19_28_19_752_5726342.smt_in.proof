(assume a2 (chain$ r$ (llist_of$ (cons$ x$ (append$ (eps$ uu$) (cons$ y$ nil$))))))
(assume a1 (not (chain$ r$ (llist_of$ (cons$ x$ (append$ (eps$ uu$) (cons$ y$ nil$)))))))
(step t3 (cl) :rule th_resolution :premises (a2 a1))
