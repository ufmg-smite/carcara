(assume a0 (not (derive$ ground_sound_Gamma$ rf$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$))))))
(assume a1 (less_eq$ (minus$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$)))) (fun_app$ rf$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$))))))
(assume a2 (=> (and (less_eq$ (minus$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))) (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))))) (less_eq$ (minus$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$)))) (fun_app$ rf$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$)))))) (derive$ ground_sound_Gamma$ rf$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$))))))
(assume a3 (less_eq$ (minus$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))) (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))))))
(step t5 (cl (and (less_eq$ (minus$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))) (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))))) (less_eq$ (minus$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$)))) (fun_app$ rf$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$)))))) (not (less_eq$ (minus$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))) (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))))))) (not (less_eq$ (minus$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$)))) (fun_app$ rf$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$))))))) :rule and_neg)
(step t6 (cl (not (and (less_eq$ (minus$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))) (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))))) (less_eq$ (minus$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$)))) (fun_app$ rf$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$))))))) (derive$ ground_sound_Gamma$ rf$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$))))) :rule implies :premises (a2))
(step t7 (cl (and (less_eq$ (minus$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))) (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))))) (less_eq$ (minus$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$)))) (fun_app$ rf$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$)))))) (not (less_eq$ (minus$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))) (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))))))) :rule resolution :premises (t5 a1))
(step t8 (cl (not (and (less_eq$ (minus$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))) (image$ concl_of$ (inferences_from$ ground_sound_Gamma$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$)))))) (less_eq$ (minus$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ (plus$ c$ (add_mset$ l$ zero$)) bot$))) (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$)))) (fun_app$ rf$ (grounding_of_clss$ subst_atm$ (sup$ cc$ (insert$ c$ bot$)))))))) :rule resolution :premises (t6 a0))
(step t9 (cl) :rule resolution :premises (t7 t8 a3))
