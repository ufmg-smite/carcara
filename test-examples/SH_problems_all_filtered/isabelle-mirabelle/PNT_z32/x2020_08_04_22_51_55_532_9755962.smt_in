; --full-saturate-quant --inst-when=full-last-call --inst-no-entail --term-db-mode=relevant --multi-trigger-linear --no-statistics --random-seed=1 --lang=smt2 --continued-execution --tlimit 30000
(set-option :produce-unsat-cores true)
(set-logic AUFLIRA)
(declare-sort Nat$ 0)
(declare-fun i$ () Nat$)
(declare-fun p$ () Nat$)
(declare-fun x$ () Real)
(declare-fun less$ (Nat$ Nat$) Bool)
(declare-fun zero$ () Nat$)
(declare-fun power$ (Nat$ Nat$) Nat$)
(declare-fun prime$ (Nat$) Bool)
(declare-fun of_nat$ (Nat$) Real)
(assert (! (not (<= (of_nat$ (power$ p$ i$)) x$)) :named a0))
(assert (! (<= (of_nat$ (power$ p$ i$)) x$) :named a1))
(assert (! (prime$ p$) :named a2))
(assert (! (less$ zero$ i$) :named a3))
(check-sat)
(get-unsat-core)
