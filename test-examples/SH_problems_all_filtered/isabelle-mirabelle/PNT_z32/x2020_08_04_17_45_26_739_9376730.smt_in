; --full-saturate-quant --inst-when=full-last-call --inst-no-entail --term-db-mode=relevant --multi-trigger-linear --no-statistics --random-seed=1 --lang=smt2 --continued-execution --tlimit 30000
(set-option :produce-unsat-cores true)
(set-logic AUFLIA)
(declare-sort Nat$ 0)
(declare-sort Num$ 0)
(declare-fun n$ () Nat$)
(declare-fun one$ () Num$)
(declare-fun bit1$ (Num$) Num$)
(declare-fun less_eq$ (Nat$ Nat$) Bool)
(declare-fun numeral$ (Num$) Nat$)
(assert (! (not (less_eq$ (numeral$ (bit1$ one$)) n$)) :named a0))
(assert (! (less_eq$ (numeral$ (bit1$ one$)) n$) :named a1))
(check-sat)
(get-unsat-core)
