(assume a0 (not (member$ x$ (atLeastLessThan$ (of_nat$ (nth_prime$ (minus$ (nat$ (floor$ (primes_pi$ x$))) one$))) (of_nat$ (nth_prime$ (nat$ (floor$ (primes_pi$ x$)))))))))
(assume a1 (member$ x$ (atLeastLessThan$ (of_nat$ (nth_prime$ n$)) (of_nat$ (nth_prime$ (suc$ n$))))))
(assume a2 (= (nat$ (floor$ (primes_pi$ x$))) (suc$ n$)))
(assume a3 (= (minus$ (nat$ (floor$ (primes_pi$ x$))) one$) n$))
(step t5 (cl (not (= x$ x$)) (not (= (atLeastLessThan$ (of_nat$ (nth_prime$ (minus$ (nat$ (floor$ (primes_pi$ x$))) one$))) (of_nat$ (nth_prime$ (nat$ (floor$ (primes_pi$ x$)))))) (atLeastLessThan$ (of_nat$ (nth_prime$ n$)) (of_nat$ (nth_prime$ (suc$ n$)))))) (not (member$ x$ (atLeastLessThan$ (of_nat$ (nth_prime$ n$)) (of_nat$ (nth_prime$ (suc$ n$)))))) (member$ x$ (atLeastLessThan$ (of_nat$ (nth_prime$ (minus$ (nat$ (floor$ (primes_pi$ x$))) one$))) (of_nat$ (nth_prime$ (nat$ (floor$ (primes_pi$ x$)))))))) :rule eq_congruent_pred)
(step t6 (cl (= x$ x$)) :rule eq_reflexive)
(step t7 (cl (not (= (atLeastLessThan$ (of_nat$ (nth_prime$ (minus$ (nat$ (floor$ (primes_pi$ x$))) one$))) (of_nat$ (nth_prime$ (nat$ (floor$ (primes_pi$ x$)))))) (atLeastLessThan$ (of_nat$ (nth_prime$ n$)) (of_nat$ (nth_prime$ (suc$ n$)))))) (not (member$ x$ (atLeastLessThan$ (of_nat$ (nth_prime$ n$)) (of_nat$ (nth_prime$ (suc$ n$)))))) (member$ x$ (atLeastLessThan$ (of_nat$ (nth_prime$ (minus$ (nat$ (floor$ (primes_pi$ x$))) one$))) (of_nat$ (nth_prime$ (nat$ (floor$ (primes_pi$ x$)))))))) :rule th_resolution :premises (t5 t6))
(step t8 (cl (not (= (of_nat$ (nth_prime$ (minus$ (nat$ (floor$ (primes_pi$ x$))) one$))) (of_nat$ (nth_prime$ n$)))) (not (= (of_nat$ (nth_prime$ (nat$ (floor$ (primes_pi$ x$))))) (of_nat$ (nth_prime$ (suc$ n$))))) (= (atLeastLessThan$ (of_nat$ (nth_prime$ (minus$ (nat$ (floor$ (primes_pi$ x$))) one$))) (of_nat$ (nth_prime$ (nat$ (floor$ (primes_pi$ x$)))))) (atLeastLessThan$ (of_nat$ (nth_prime$ n$)) (of_nat$ (nth_prime$ (suc$ n$)))))) :rule eq_congruent)
(step t9 (cl (not (= (nth_prime$ (minus$ (nat$ (floor$ (primes_pi$ x$))) one$)) (nth_prime$ n$))) (= (of_nat$ (nth_prime$ (minus$ (nat$ (floor$ (primes_pi$ x$))) one$))) (of_nat$ (nth_prime$ n$)))) :rule eq_congruent)
(step t10 (cl (not (= (minus$ (nat$ (floor$ (primes_pi$ x$))) one$) n$)) (= (nth_prime$ (minus$ (nat$ (floor$ (primes_pi$ x$))) one$)) (nth_prime$ n$))) :rule eq_congruent)
(step t11 (cl (= (of_nat$ (nth_prime$ (minus$ (nat$ (floor$ (primes_pi$ x$))) one$))) (of_nat$ (nth_prime$ n$))) (not (= (minus$ (nat$ (floor$ (primes_pi$ x$))) one$) n$))) :rule th_resolution :premises (t9 t10))
(step t12 (cl (not (= (of_nat$ (nth_prime$ (nat$ (floor$ (primes_pi$ x$))))) (of_nat$ (nth_prime$ (suc$ n$))))) (= (atLeastLessThan$ (of_nat$ (nth_prime$ (minus$ (nat$ (floor$ (primes_pi$ x$))) one$))) (of_nat$ (nth_prime$ (nat$ (floor$ (primes_pi$ x$)))))) (atLeastLessThan$ (of_nat$ (nth_prime$ n$)) (of_nat$ (nth_prime$ (suc$ n$))))) (not (= (minus$ (nat$ (floor$ (primes_pi$ x$))) one$) n$))) :rule th_resolution :premises (t8 t11))
(step t13 (cl (not (= (nth_prime$ (nat$ (floor$ (primes_pi$ x$)))) (nth_prime$ (suc$ n$)))) (= (of_nat$ (nth_prime$ (nat$ (floor$ (primes_pi$ x$))))) (of_nat$ (nth_prime$ (suc$ n$))))) :rule eq_congruent)
(step t14 (cl (not (= (nat$ (floor$ (primes_pi$ x$))) (suc$ n$))) (= (nth_prime$ (nat$ (floor$ (primes_pi$ x$)))) (nth_prime$ (suc$ n$)))) :rule eq_congruent)
(step t15 (cl (= (of_nat$ (nth_prime$ (nat$ (floor$ (primes_pi$ x$))))) (of_nat$ (nth_prime$ (suc$ n$)))) (not (= (nat$ (floor$ (primes_pi$ x$))) (suc$ n$)))) :rule th_resolution :premises (t13 t14))
(step t16 (cl (= (atLeastLessThan$ (of_nat$ (nth_prime$ (minus$ (nat$ (floor$ (primes_pi$ x$))) one$))) (of_nat$ (nth_prime$ (nat$ (floor$ (primes_pi$ x$)))))) (atLeastLessThan$ (of_nat$ (nth_prime$ n$)) (of_nat$ (nth_prime$ (suc$ n$))))) (not (= (minus$ (nat$ (floor$ (primes_pi$ x$))) one$) n$)) (not (= (nat$ (floor$ (primes_pi$ x$))) (suc$ n$)))) :rule th_resolution :premises (t12 t15))
(step t17 (cl (not (member$ x$ (atLeastLessThan$ (of_nat$ (nth_prime$ n$)) (of_nat$ (nth_prime$ (suc$ n$)))))) (member$ x$ (atLeastLessThan$ (of_nat$ (nth_prime$ (minus$ (nat$ (floor$ (primes_pi$ x$))) one$))) (of_nat$ (nth_prime$ (nat$ (floor$ (primes_pi$ x$))))))) (not (= (minus$ (nat$ (floor$ (primes_pi$ x$))) one$) n$)) (not (= (nat$ (floor$ (primes_pi$ x$))) (suc$ n$)))) :rule th_resolution :premises (t7 t16))
(step t18 (cl) :rule resolution :premises (t17 a0 a1 a2 a3))
