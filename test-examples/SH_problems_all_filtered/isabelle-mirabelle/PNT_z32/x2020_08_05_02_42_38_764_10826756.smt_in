; --full-saturate-quant --inst-when=full-last-call --inst-no-entail --term-db-mode=relevant --multi-trigger-linear --no-statistics --random-seed=1 --lang=smt2 --continued-execution --tlimit 30000
(set-option :produce-unsat-cores true)
(set-logic AUFLIRA)
(declare-sort Complex$ 0)
(declare-sort Real_set$ 0)
(declare-sort Real_filter$ 0)
(declare-sort Real_complex_fun$ 0)
(declare-fun f$ (Complex$) Complex$)
(declare-fun g$ () Real_complex_fun$)
(declare-fun x$ () Real)
(declare-fun f$a (Complex$) Complex$)
(declare-fun g$a () Real_complex_fun$)
(declare-fun top$ () Real_set$)
(declare-fun times$ (Complex$ Complex$) Complex$)
(declare-fun fun_app$ (Real_complex_fun$ Real) Complex$)
(declare-fun at_within$ (Real Real_set$) Real_filter$)
(declare-fun vector_derivative$ (Real_complex_fun$ Real_filter$) Complex$)
(assert (! (not (= (times$ (f$ (fun_app$ g$ x$)) (vector_derivative$ g$ (at_within$ x$ top$))) (times$ (f$a (fun_app$ g$a x$)) (vector_derivative$ g$a (at_within$ x$ top$))))) :named a0))
(assert (! (= g$a g$) :named a1))
(assert (! (= (f$a (fun_app$ g$a x$)) (f$ (fun_app$ g$a x$))) :named a2))
(check-sat)
(get-unsat-core)
