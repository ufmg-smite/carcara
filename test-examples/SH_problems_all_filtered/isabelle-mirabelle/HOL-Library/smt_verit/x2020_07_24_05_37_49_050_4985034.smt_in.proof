(assume a0 (not (less_eq$ (fst$ x$) (fst$ y$))))
(assume a1 (and (less_eq$ (fst$ x$) (fst$ y$)) (less_eq$a (snd$ x$) (snd$ y$))))
(step t3 (cl (less_eq$ (fst$ x$) (fst$ y$))) :rule and :premises (a1))
(step t4 (cl) :rule resolution :premises (t3 a0))
