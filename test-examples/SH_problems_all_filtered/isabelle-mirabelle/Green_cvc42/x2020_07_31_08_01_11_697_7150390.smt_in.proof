(assume a1 (not (= (inner$ (vector_derivative$ gamma$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))) i$) (vector_derivative$a uu$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))))))
(assume a2 (= (vector_derivative$a uu$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))) (vector_derivative$a uu$ (at_within$ x$ top$))))
(assume a3 (= (vector_derivative$ gamma$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))) (vector_derivative$ gamma$ (at_within$ x$ top$))))
(assume a4 (= (vector_derivative$a uu$ (at_within$ x$ top$)) (inner$ (vector_derivative$ gamma$ (at_within$ x$ top$)) i$)))
(step t5 (cl (not (= (inner$ (vector_derivative$ gamma$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))) i$) (inner$ (vector_derivative$ gamma$ (at_within$ x$ top$)) i$))) (not (= (vector_derivative$a uu$ (at_within$ x$ top$)) (inner$ (vector_derivative$ gamma$ (at_within$ x$ top$)) i$))) (not (= (vector_derivative$a uu$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))) (vector_derivative$a uu$ (at_within$ x$ top$)))) (= (inner$ (vector_derivative$ gamma$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))) i$) (vector_derivative$a uu$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))))) :rule eq_transitive)
(step t6 (cl (not (= (vector_derivative$ gamma$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))) (vector_derivative$ gamma$ (at_within$ x$ top$)))) (not (= i$ i$)) (= (inner$ (vector_derivative$ gamma$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))) i$) (inner$ (vector_derivative$ gamma$ (at_within$ x$ top$)) i$))) :rule eq_congruent)
(step t7 (cl (= i$ i$)) :rule eq_reflexive)
(step t8 (cl (not (= (vector_derivative$ gamma$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))) (vector_derivative$ gamma$ (at_within$ x$ top$)))) (= (inner$ (vector_derivative$ gamma$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))) i$) (inner$ (vector_derivative$ gamma$ (at_within$ x$ top$)) i$))) :rule th_resolution :premises (t6 t7))
(step t9 (cl (not (= (vector_derivative$a uu$ (at_within$ x$ top$)) (inner$ (vector_derivative$ gamma$ (at_within$ x$ top$)) i$))) (not (= (vector_derivative$a uu$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))) (vector_derivative$a uu$ (at_within$ x$ top$)))) (= (inner$ (vector_derivative$ gamma$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))) i$) (vector_derivative$a uu$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0)))) (not (= (vector_derivative$ gamma$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))) (vector_derivative$ gamma$ (at_within$ x$ top$))))) :rule th_resolution :premises (t5 t8))
(step t10 (cl) :rule resolution :premises (t9 a1 a2 a3 a4))
