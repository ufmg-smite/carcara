; --full-saturate-quant --inst-when=full-last-call --inst-no-entail --term-db-mode=relevant --multi-trigger-linear --no-statistics --random-seed=1 --lang=smt2 --continued-execution --tlimit 30000
(set-option :produce-unsat-cores true)
(set-logic AUFLIRA)
(declare-sort A$ 0)
(declare-sort Real_set$ 0)
(declare-sort Real_a_fun$ 0)
(declare-sort Real_filter$ 0)
(declare-sort Real_real_fun$ 0)
(declare-fun i$ () A$)
(declare-fun x$ () Real)
(declare-fun uu$ () Real_real_fun$)
(declare-fun top$ () Real_set$)
(declare-fun gamma$ () Real_a_fun$)
(declare-fun inner$ (A$ A$) Real)
(declare-fun fun_app$ (Real_real_fun$ Real) Real)
(declare-fun fun_app$a (Real_a_fun$ Real) A$)
(declare-fun at_within$ (Real Real_set$) Real_filter$)
(declare-fun atLeastAtMost$ (Real Real) Real_set$)
(declare-fun vector_derivative$ (Real_a_fun$ Real_filter$) A$)
(declare-fun vector_derivative$a (Real_real_fun$ Real_filter$) Real)
(assert (! (forall ((?v0 Real)) (! (= (fun_app$ uu$ ?v0) (inner$ (fun_app$a gamma$ ?v0) i$)) :pattern ((fun_app$ uu$ ?v0)))) :named a0))
(assert (! (not (= (inner$ (vector_derivative$ gamma$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))) i$) (vector_derivative$a uu$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))))) :named a1))
(assert (! (= (vector_derivative$a uu$ (at_within$ x$ top$)) (vector_derivative$a uu$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0)))) :named a2))
(assert (! (= (vector_derivative$ gamma$ (at_within$ x$ (atLeastAtMost$ 0.0 1.0))) (vector_derivative$ gamma$ (at_within$ x$ top$))) :named a3))
(assert (! (= (vector_derivative$a uu$ (at_within$ x$ top$)) (inner$ (vector_derivative$ gamma$ (at_within$ x$ top$)) i$)) :named a4))
(check-sat)
(get-unsat-core)
