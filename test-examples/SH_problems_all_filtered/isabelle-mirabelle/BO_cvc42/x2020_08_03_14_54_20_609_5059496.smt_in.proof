(assume a3 (= (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ (app$ s$ t$)))) (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ t$)))))
(assume a2 (not (= (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ (app$ s$ t$)))) (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ t$))))))
(step t3 (cl) :rule th_resolution :premises (a3 a2))
