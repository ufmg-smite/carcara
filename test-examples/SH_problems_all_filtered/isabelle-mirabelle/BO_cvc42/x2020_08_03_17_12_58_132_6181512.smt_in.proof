(assume a0 (not (= (head$ ta$) (head$ sa$))))
(assume a1 (= (head$ ta$) (sym$ g$)))
(assume a2 (= (head$ sa$) (sym$ f$)))
(assume a3 (= g$ f$))
(step t5 (cl (not (= (head$ ta$) (sym$ g$))) (not (= (sym$ g$) (sym$ f$))) (not (= (head$ sa$) (sym$ f$))) (= (head$ ta$) (head$ sa$))) :rule eq_transitive)
(step t6 (cl (not (= g$ f$)) (= (sym$ g$) (sym$ f$))) :rule eq_congruent)
(step t7 (cl (not (= (head$ ta$) (sym$ g$))) (not (= (head$ sa$) (sym$ f$))) (= (head$ ta$) (head$ sa$)) (not (= g$ f$))) :rule th_resolution :premises (t5 t6))
(step t8 (cl) :rule resolution :premises (t7 a0 a1 a2 a3))
