(assume a2 (not (= (plus$ (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ (app$ s$ t$)))) (zHMSet$ (zmset_of$ (hmsetmset$ (of_nat$ delta$))))) (plus$ (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ s$))) (zHMSet$ (zmset_of$ (hmsetmset$ (of_nat$ delta$))))))))
(assume a3 (= (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ (app$ s$ t$)))) (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ s$)))))
(step t3 (cl (not (= (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ (app$ s$ t$)))) (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ s$))))) (not (= (zHMSet$ (zmset_of$ (hmsetmset$ (of_nat$ delta$)))) (zHMSet$ (zmset_of$ (hmsetmset$ (of_nat$ delta$)))))) (= (plus$ (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ (app$ s$ t$)))) (zHMSet$ (zmset_of$ (hmsetmset$ (of_nat$ delta$))))) (plus$ (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ s$))) (zHMSet$ (zmset_of$ (hmsetmset$ (of_nat$ delta$))))))) :rule eq_congruent)
(step t4 (cl (= (zHMSet$ (zmset_of$ (hmsetmset$ (of_nat$ delta$)))) (zHMSet$ (zmset_of$ (hmsetmset$ (of_nat$ delta$)))))) :rule eq_reflexive)
(step t5 (cl (not (= (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ (app$ s$ t$)))) (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ s$))))) (= (plus$ (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ (app$ s$ t$)))) (zHMSet$ (zmset_of$ (hmsetmset$ (of_nat$ delta$))))) (plus$ (eval_tpoly$ a$ (map_tpoly$ uu$ uua$ (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ coef_sym$ s$))) (zHMSet$ (zmset_of$ (hmsetmset$ (of_nat$ delta$))))))) :rule th_resolution :premises (t3 t4))
(step t6 (cl) :rule resolution :premises (t5 a2 a3))
