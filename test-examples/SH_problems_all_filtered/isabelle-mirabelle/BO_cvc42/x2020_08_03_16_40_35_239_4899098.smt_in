; --full-saturate-quant --inst-when=full-last-call --inst-no-entail --term-db-mode=relevant --multi-trigger-linear --no-statistics --random-seed=1 --lang=smt2 --continued-execution --tlimit 30000
(set-option :produce-unsat-cores true)
(set-logic AUFLIA)
(declare-sort Nat$ 0)
(declare-sort S_v_tm$ 0)
(declare-sort S_nat_fun$ 0)
(declare-sort S_enat_fun$ 0)
(declare-sort V_s_set_fun$ 0)
(declare-fun sa$ () S_v_tm$)
(declare-fun ta$ () S_v_tm$)
(declare-fun ua$ () S_v_tm$)
(declare-fun wt$ (V_s_set_fun$ Nat$ S_enat_fun$ S_nat_fun$ S_v_tm$) Nat$)
(declare-fun delta$ () Nat$)
(declare-fun wt_sym$ () S_nat_fun$)
(declare-fun arity_sym$ () S_enat_fun$)
(declare-fun ground_heads_var$ () V_s_set_fun$)
(assert (! (not (= (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ ua$) (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ sa$))) :named a0))
(assert (! (= (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ ta$) (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ sa$)) :named a1))
(assert (! (= (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ ua$) (wt$ ground_heads_var$ delta$ arity_sym$ wt_sym$ ta$)) :named a2))
(check-sat)
(get-unsat-core)
